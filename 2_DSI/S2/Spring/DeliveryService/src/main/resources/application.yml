server:
  port: 8015
spring:
  application:
    name: DeliveryService
  datasource:
    driver-class-name: org.postgresql.Driver
    password: Kayto404
    url: jdbc:postgresql://localhost:5432/dbDilvery
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true