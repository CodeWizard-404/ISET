<form method="post" action="{{ path('app_institut_delete', {'id': institut.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ institut.id) }}">
    <button class="btn">Delete</button>
</form>
